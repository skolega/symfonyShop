<ul class="list-group">
    {% for category in categories %}
        <a href="{{ path('products_list', { 'id':category.id }) }}"><li class="list-group-item">{{ category.name }}</li></a>
    {% endfor %}
</ul>
